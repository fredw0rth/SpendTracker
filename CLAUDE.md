# SpendTracker

## Language

All user-facing text in the app (button labels, headings, placeholders, settings copy) must use
**British English** — colour, categorise, customisation, organise, etc. — not American spelling.
Code identifiers (variable/function names) don't need to match; only what the user actually reads
in the UI matters.
